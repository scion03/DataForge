For Running the code:
```
g++ -std=c++20 -stdlib=libc++ db.cpp -o db && db database_file.db
```

For Testing:
1. Install dependencies:
```
Bundle Install
```
2. Running the tests:
```
rspec test_spec.rb
```